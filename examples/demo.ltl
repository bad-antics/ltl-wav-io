// examples/demo.ltl — runnable demo for ltl_wav_io

import std::io
use ltl_wav_io as lib

fn main() {
    io::println("ltl_wav_io demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}