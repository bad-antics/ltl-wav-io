// ltl-wav-io/main.ltl
//
// WAV (RIFF) encoder / decoder for PCM16 and PCM-float.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-wav-io v0.1.0")
    io::println("WAV (RIFF) encoder / decoder for PCM16 and PCM-float.")
    0
}
